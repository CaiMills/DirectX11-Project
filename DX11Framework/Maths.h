#pragma once
#include "Structures.h"

/// <summary>
/// This is just the base to include all my maths calculation rather than needing to type repeated code
/// </summary>

class Maths
{
public:
	float Dot(Vector3 a, Vector3 b);
};

